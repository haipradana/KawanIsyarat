include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - cactus-main/**
    - lib/core/ffi/cactus.dart
  errors:
    deprecated_member_use: ignore

linter:
  rules:
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
    avoid_print: false
